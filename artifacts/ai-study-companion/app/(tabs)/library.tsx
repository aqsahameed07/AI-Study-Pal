import React, { useState } from 'react';
import { Modal, Pressable, StyleSheet, Text, TextInput, View } from 'react-native';
import * as Haptics from 'expo-haptics';
import { Ionicons } from '@expo/vector-icons';
import { useRouter } from 'expo-router';
import { Screen, SectionTitle, SoftButton, Surface, ui } from '@/components/StudyUI';
import { useColors } from '@/hooks/useColors';
import { useStudy } from '@/context/StudyContext';

export default function LibraryScreen() {
  const colors = useColors(); const router = useRouter(); const { sets, addSet } = useStudy(); const [open, setOpen] = useState(false); const [title, setTitle] = useState(''); const [subject, setSubject] = useState('');
  const create = async () => { if (!title.trim() || !subject.trim()) return; await addSet({ id: Date.now().toString(), title: title.trim(), subject: subject.trim(), createdAt: new Date().toISOString(), cards: [{ front: `What is the core idea of ${title.trim()}?`, back: 'Write the answer in your own words, then connect it to an example.' }, { front: 'What should you remember first?', back: 'Start with the concept, then check your recall without looking.' }] }); Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success); setOpen(false); setTitle(''); setSubject(''); };
  return <Screen><View style={styles.header}><View><Text style={[styles.kicker, { color: colors.accent }]}>YOUR MATERIALS</Text><Text style={[styles.title, { color: colors.foreground }]}>Library</Text></View><Pressable onPress={() => setOpen(true)} style={[styles.add, { backgroundColor: colors.primary }]}><Ionicons name="add" size={22} color={colors.primaryForeground} /></Pressable></View><View style={[styles.search, { backgroundColor: colors.secondary }]}><Ionicons name="search" size={18} color={colors.mutedForeground} /><Text style={[styles.searchText, { color: colors.mutedForeground }]}>Search your study space</Text></View><SectionTitle title="Continue learning" /><Surface style={styles.feature} accent><View style={[styles.fileIcon, { backgroundColor: colors.secondary }]}><Ionicons name="document-text-outline" size={23} color={colors.primary} /></View><View style={{ flex: 1 }}><Text style={[styles.cardTitle, { color: colors.foreground }]}>Cellular respiration notes</Text><Text style={[styles.detail, { color: colors.mutedForeground }]}>Biology · 24 pages · Updated today</Text><View style={{ marginTop: 11 }}><SoftButton label="Open notes" icon="arrow-forward" onPress={() => router.push('/quiz')} /></View></View></Surface><SectionTitle title="Flashcard sets" action="Create set" onAction={() => setOpen(true)} />{sets.length === 0 ? <Surface style={styles.empty}><Ionicons name="layers-outline" size={28} color={colors.accent} /><Text style={[styles.cardTitle, { color: colors.foreground }]}>Your first set starts here</Text><Text style={[styles.detail, { color: colors.mutedForeground }]}>Turn a tricky topic into a quick review loop.</Text><SoftButton label="Create flashcards" onPress={() => setOpen(true)} icon="add" /></Surface> : sets.map(set => <Pressable key={set.id} onPress={() => router.push('/flashcards')}><Surface style={styles.row}><View style={[styles.fileIcon, { backgroundColor: colors.secondary }]}><Ionicons name="layers-outline" size={21} color={colors.accent} /></View><View style={{ flex: 1 }}><Text style={[styles.cardTitle, { color: colors.foreground }]}>{set.title}</Text><Text style={[styles.detail, { color: colors.mutedForeground }]}>{set.subject} · {set.cards.length} cards</Text></View><Ionicons name="chevron-forward" size={17} color={colors.mutedForeground} /></Surface></Pressable>)}<SectionTitle title="Practice" /><Pressable onPress={() => router.push('/quiz')}><Surface style={styles.row}><View style={[styles.fileIcon, { backgroundColor: colors.secondary }]}><Ionicons name="checkmark-circle-outline" size={21} color={colors.primary} /></View><View style={{ flex: 1 }}><Text style={[styles.cardTitle, { color: colors.foreground }]}>Photosynthesis checkpoint</Text><Text style={[styles.detail, { color: colors.mutedForeground }]}>10 questions · 5 min</Text></View><Ionicons name="play" size={17} color={colors.primary} /></Surface></Pressable><Modal visible={open} transparent animationType="slide" onRequestClose={() => setOpen(false)}><View style={styles.modalBackdrop}><View style={[styles.modal, { backgroundColor: colors.card }]}><View style={styles.modalHeader}><Text style={[styles.title, { color: colors.foreground }]}>New flashcard set</Text><Pressable onPress={() => setOpen(false)}><Ionicons name="close" size={23} color={colors.mutedForeground} /></Pressable></View><Text style={[styles.detail, { color: colors.mutedForeground }]}>Give your next review loop a clear name.</Text><TextInput value={title} onChangeText={setTitle} placeholder="Set title" placeholderTextColor={colors.mutedForeground} style={[ui.input, { color: colors.foreground, borderColor: colors.border, marginTop: 18 }]} /><TextInput value={subject} onChangeText={setSubject} placeholder="Subject" placeholderTextColor={colors.mutedForeground} style={[ui.input, { color: colors.foreground, borderColor: colors.border, marginTop: 10 }]} /><View style={{ marginTop: 16 }}><SoftButton label="Create set" icon="checkmark" onPress={create} disabled={!title.trim() || !subject.trim()} /></View></View></View></Modal></Screen>;
}
const styles = StyleSheet.create({ header: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', marginBottom: 18 }, kicker: { fontFamily: 'Inter_700Bold', fontSize: 10, letterSpacing: 1.3 }, title: { fontFamily: 'Inter_700Bold', fontSize: 27, letterSpacing: -0.7 }, add: { width: 44, height: 44, borderRadius: 14, justifyContent: 'center', alignItems: 'center' }, search: { borderRadius: 14, padding: 14, flexDirection: 'row', gap: 9, marginBottom: 27 }, searchText: { fontFamily: 'Inter_400Regular', fontSize: 13 }, feature: { flexDirection: 'row', gap: 13, marginBottom: 24 }, fileIcon: { width: 43, height: 43, borderRadius: 13, alignItems: 'center', justifyContent: 'center' }, cardTitle: { fontFamily: 'Inter_600SemiBold', fontSize: 14 }, detail: { fontFamily: 'Inter_400Regular', fontSize: 12, lineHeight: 18, marginTop: 3 }, row: { flexDirection: 'row', alignItems: 'center', gap: 13, marginBottom: 9 }, empty: { alignItems: 'center', gap: 8, paddingVertical: 23, marginBottom: 24 }, modalBackdrop: { flex: 1, backgroundColor: 'rgba(7,19,41,0.75)', justifyContent: 'flex-end' }, modal: { padding: 20, borderTopLeftRadius: 26, borderTopRightRadius: 26, minHeight: 300 }, modalHeader: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center' } });